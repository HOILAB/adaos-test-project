# ADAOS Test Project Conventions

- Use semantic HTML
- Keep code simple and readable
- Test all changes before committing
- This project uses a Node.js HTTP server for serving both static files and API endpoints
